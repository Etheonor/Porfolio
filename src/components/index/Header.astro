<header class="header">
  <div class="header__text">
    <h1 class="title">
      Hi,<br />
      I'm <span class="name">Michael</span>
    </h1>
    <h2 class="subtitle">Want to build a website?</h2>
  </div>
  <img
    src="./nodeboy.svg"
    class="nodeboy"
    aria-labelledby="A developper in front of his computer"
    aria-label="A developper in front of his computer"
    title="A developper in front of his computer"
    width="400"
    height="400"
  />
</header>

<style>
  .header {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
  }
  .header__text {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }
  .title {
    font-style: italic;
    font-size: 3rem;
    margin-bottom: 0;
  }

  .name {
    font-weight: bold;
    background-color: var(--accent-color);
  }
  .subtitle {
    color: var(--secondary-color);
    font-weight: 600;
  }

  .nodeboy {
    max-width: 300px;
  }

  @media (min-width: 768px) {
    .header {
      flex-direction: row;
    }
  }

  @media (min-width: 1200px) {
    .header {
      gap: 6rem;
      width: 100%;
      height: 35rem;
      align-content: end;
      flex-wrap: wrap;
      margin-bottom: 5rem;
    }
    .title {
      font-size: 5rem;
    }
    .subtitle {
      font-size: 1.8rem;
    }
    .nodeboy {
      max-width: 400px;
    }
  }
</style>
